package polsl.pl.tab.api.atractionEntry.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import polsl.pl.tab.api.atractionEntry.model.AtractionEntry;

public interface AtractionEntryRepository extends JpaRepository<AtractionEntry, Integer> {
}
