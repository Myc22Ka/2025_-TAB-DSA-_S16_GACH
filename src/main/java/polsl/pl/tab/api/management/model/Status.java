package polsl.pl.tab.api.management.model;

/**
 * Enum representing the status of a role change request.
 * The status indicates the current state of the request in the system.
 */
public enum Status {
    PENDING,
    APPROVED,
    REJECTED
}
